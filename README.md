# file-compression-tool
It is a file compression and decompression tool built using C and HTML/CSS/JavaScript. The project uses algorithms like Huffman Coding and Run-Length Encoding (RLE) to reduce file size efficiently while maintaining lossless data recovery through a simple upload/download interface.
